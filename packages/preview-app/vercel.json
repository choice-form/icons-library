{
  "buildCommand": "cd ../.. && pnpm install && cd packages/icons-react && pnpm build && cd ../preview-app && pnpm build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
